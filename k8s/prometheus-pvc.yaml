apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: prometheus-pvc
 namespace: workloads-lab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 5Gi
